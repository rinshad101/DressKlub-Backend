server:
  port: 8083

spring:
  application:
    name: CART-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/dressklub_cartdb
    username: root
    password: rinshad3657
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka